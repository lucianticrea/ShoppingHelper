@model ShoppingHelper.TipProdus

@{
    ViewBag.Title = "Delete";
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>
<fieldset>
    <legend>TipProdus</legend>

    <div class="display-label">Denumire</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.Denumire)
    </div>

    <div class="display-label">UnitateMasura</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.UnitateMasura)
    </div>
</fieldset>
@using (Html.BeginForm()) {
    <p>
        <input type="submit" value="Delete" /> |
        @Html.ActionLink("Back to List", "Index")
    </p>
}
