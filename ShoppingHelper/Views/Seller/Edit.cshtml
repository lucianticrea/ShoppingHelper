@model ShoppingHelper.Produs

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

<script src="@Url.Content("~/Scripts/jquery.validate.min.js")" type="text/javascript"></script>
<script src="@Url.Content("~/Scripts/jquery.validate.unobtrusive.min.js")" type="text/javascript"></script>

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)
    <fieldset>
        <legend>Produs</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.Denumire)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.Denumire)
            @Html.ValidationMessageFor(model => model.Denumire)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.UnitateMasura)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.UnitateMasura)
            @Html.ValidationMessageFor(model => model.UnitateMasura)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>
