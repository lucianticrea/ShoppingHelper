@model ShoppingHelper.Produs

@{
    ViewBag.Title = "Delete";
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>
<fieldset>
    <legend>Produs</legend>

    <div class="display-label">Magazin</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.Magazin.Nume)
    </div>

    <div class="display-label">Tip Produs</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.TipProdus.Denumire)
    </div>

    <div class="display-label">Pret</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.Pret)
    </div>
</fieldset>
@using (Html.BeginForm()) {
    <p>
        <input type="submit" value="Delete" /> |
        @Html.ActionLink("Back to List", "Index")
    </p>
}
